import 'package:flutter/material.dart';

class CancelledEventsPage extends StatelessWidget {
  const CancelledEventsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Bekor qilganlarim Page'),
    );
  }
}
