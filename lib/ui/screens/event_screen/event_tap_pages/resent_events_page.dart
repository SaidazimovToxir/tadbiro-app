import 'package:flutter/material.dart';

class RecentEventsPage extends StatelessWidget {
  const RecentEventsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Yaqinda Page'),
    );
  }
}
